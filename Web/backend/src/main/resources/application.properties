spring.application.name=backend
spring.datasource.url=jdbc:postgresql://postgres:5432/eduaircontrol
spring.datasource.username=eduair_user
spring.datasource.password=eduair_pass

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

jwt.secret=${JWT_SECRET:mi_clave_super_segura_de_mas_de_32_caracteres}
jwt.expiration=${JWT_EXPIRATION:86400000}

spring.security.user.name = admin
spring.security.user.password = 1234
spring.security.user.role = ADMIN

spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google.scope=openid,email,profile
app.frontend.oauth2-success-url=${FRONTEND_OAUTH2_SUCCESS_URL:http://localhost:3000/oauth2/success}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
